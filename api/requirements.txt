# Slim, Vercel-only dependencies for the Python serverless function.
# The full backend/requirements.txt is intentionally NOT used on Vercel
# because it contains heavy dev-only libs (pandas, numpy, boto3, ...).
fastapi==0.110.1
pydantic>=2.6.4
motor==3.3.1
pymongo==4.6.3
