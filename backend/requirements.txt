# Backend deps.
# NOTE: kept intentionally small so Vercel Services builds fast.
# Extras used only for local scripts (pandas / boto3 / …) have been removed.
# Add them back here only if you actually import them from server.py.
fastapi==0.110.1
uvicorn==0.25.0
pydantic>=2.6.4
python-dotenv>=1.0.1
motor==3.3.1
pymongo==4.6.3
